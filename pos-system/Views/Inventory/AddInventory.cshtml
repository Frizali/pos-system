@model pos_system.Models.InventoryFormModel
@{
    ViewData["Title"] = "Add Inventory Item";
}

<link rel="stylesheet" href="~/css/Product/index.css" asp-append-version="true" />
@using (Html.BeginForm("AddInventory", "Inventory", FormMethod.Post))
{
    <div class="container d-flex flex-column gap-3">
        <div class="card border-0">
            <div class="card-body">
                <h5 class="card-title">Inventory Form</h5>
                <hr />

                <div class="mb-3">
                    <label for="name" class="form-label">Nama Barang *</label>
                    <input class="form-control" id="name" name="Part.PartName" placeholder="Contoh: Tepung Terigu" required>
                </div>

                <div class="mb-3">
                    <label for="category" class="form-label">Kategori Barang *</label>
                    <select class="form-select" id="category" name="Part.PartTypeId" required>
                        <option selected disabled>Pilih kategori</option>
                        @foreach(var type in Model.PartTypes)
                        {
                            <option value="@type.PartTypeId">@type.PartTypeName</option>                            
                        }
                    </select>
                </div>

                <div class="mb-3">
                    <label for="stock" class="form-label">Jumlah Barang *</label>
                    <input type="number" class="form-control" id="stock" name="Part.PartQty" required>
                </div>

                <div class="mb-3">
                    <label for="unit" class="form-label">Unit *</label>
                    <select class="form-select" id="unit" name="Part.UnitId" required>
                        <option selected disabled>Pilih unit</option>
                        @foreach(var unit in Model.Units)
                        {
                            <option value="@unit.UnitId">@unit.UnitName</option>
                        }
                    </select>
                </div>

                <div class="mb-3">
                    <label for="price" class="form-label">Harga (Rp) *</label>
                    <div class="input-group">
                        <span class="input-group-text">Rp</span>
                        <input type="number" class="form-control" id="price" name="Part.Price" placeholder="Contoh: 5000" required>
                    </div>
                </div>

                <div class="mb-3">
                    <label for="note" class="form-label">Catatan</label>
                    <textarea class="form-control" id="note" name="Part.Note" rows="3" placeholder="Contoh: Barang harus disimpan di tempat kering."></textarea>
                </div>

                <div class="mt-4">
                    <button type="submit" class="btn btn-primary w-100">Simpan Barang</button>
                </div>
            </div>
        </div>
    </div>
}
